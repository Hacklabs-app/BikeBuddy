// lib/features/customer/booking/group_booking_screen.dart
import 'package:flutter/material.dart';

class GroupBookingScreen extends StatelessWidget {
  const GroupBookingScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Group Booking — coming soon')));
}
